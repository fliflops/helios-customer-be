const rataModels = require('./rata');
const tmsModels = require('./tms');
const heliosModels = require('./helios');
const models = require('./helios_customer');


module.exports = {
    rataModels,
    models,
    tmsModels,
    heliosModels   
}